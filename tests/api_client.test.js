const test=require("node:test"),assert=require("node:assert/strict"),http=require("node:http");const {callAPI}=require("../src/api_client");
test("HTTP client handles success and HTTP error",async t=>{const server=http.createServer((req,res)=>{if(req.url==="/error"){res.writeHead(500,{"content-type":"application/json"});res.end(JSON.stringify({error:"boom"}));}else{res.writeHead(200,{"content-type":"application/json"});res.end(JSON.stringify({ok:true,method:req.method}));}});await new Promise(r=>server.listen(0,"127.0.0.1",r));t.after(()=>server.close());const port=server.address().port;assert.deepEqual(await callAPI("http://127.0.0.1:"+port+"/"),{ok:true,status:200,data:{ok:true,method:"GET"},attempt:0});assert.deepEqual(await callAPI("http://127.0.0.1:"+port+"/error"),{ok:false,status:500,data:{error:"boom"},attempt:0});});