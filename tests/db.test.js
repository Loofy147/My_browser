const test=require("node:test"),assert=require("node:assert/strict");const {openDB,recordStep,getRun,allRuns}=require("../src/db");
test("SQLite round-trip and idempotency",()=>{const db=openDB();recordStep(db,{runLabel:"r",source:"s",step:"x",result:{nested:{answer:42}},status:"success"});recordStep(db,{runLabel:"r",source:"s",step:"x",result:{nested:{answer:99}},status:"updated"});const rows=getRun(db,"r");assert.equal(rows.length,1);assert.deepEqual(rows[0].result,{nested:{answer:99}});assert.equal(rows[0].status,"updated");assert.equal(typeof rows[0].created_at,"string");assert.equal(allRuns(db).length,1);db.close();});